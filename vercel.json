{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf .vercel-static && mkdir -p .vercel-static && cp -R prototypes/nexcent-static/. .vercel-static/",
  "outputDirectory": ".vercel-static"
}
